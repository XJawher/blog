/**
 * @description
 * @author lipc
 * @date 20/03/2021
 * 快速排序有很多种算法实现， optimization 的版本有很多，这里先写一个最基础的
 */
function quickBase (arr, left, right) {
    if (!arr || arr.length) return [];
    if (arr.length === 1) return arr;


}